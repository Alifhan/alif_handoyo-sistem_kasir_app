READ AND WRITE "NAME ITEM" WITH STRING
READ AND WRITE "PRICE" WITH NUMBER
READ AND WRITE "TOTAL ITEM" WITH NUMBER
STORE "AMOUNT" AS INTEGER WITH VALUE
STORE "PRICE" AS INTEGER WITH VALUE
STORE "TOTAL" AS INTEGER WITH 0
STORE "TOTAL PRICE" AS INTEGER WITH 0
IF "Buyers want to add items"
    PRINT "INPUT ITEM"
ELSE 
    PRINT "INPUT PAYMENT MONEY"
END IF